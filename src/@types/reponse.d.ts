interface User {
  id: number;
  name: string;
  email: string;
}

interface TransactionOverView {
  name: string;
  value: number;
}

interface NetIncomeData {
  name: string;
  income: number;
  expense: number;
}
